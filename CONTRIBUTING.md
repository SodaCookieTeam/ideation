# 기여 방법

아이디어를 올리고 고치는 절차입니다. 문서 자체를 어떻게 쓰는지는 [CONVENTIONS.md](CONVENTIONS.md) 를 보세요.

이 레포의 목적은 **빨리 많이 쌓는 것**입니다. 절차 때문에 아이디어를 못 올리는 상황이 생기면 절차가 틀린 겁니다. 그럴 땐 규칙을 바꾸는 PR을 올려주세요.

## 전체 흐름

```
생각남 → Issue 하나 던지기 → (살릴 만하면) 브랜치 → ideas/ 문서 작성 → PR → 머지
```

Issue 단계는 건너뛰어도 됩니다. 이미 정리된 아이디어면 바로 문서로 쓰세요.

## 브랜치

`main` 에 직접 커밋하지 않습니다. 브랜치를 따서 PR로 올립니다.

| 접두어 | 언제 | 예시 |
| --- | --- | --- |
| `idea/` | 아이디어 문서 추가·수정 | `idea/lecture-summarizer` |
| `research/` | 조사·기술 검증 자료 | `research/whisper-api` |
| `docs/` | README, 규칙 등 운영 문서 | `docs/readme` |
| `chore/` | 그 외 (설정, 정리) | `chore/gitignore` |

머지된 브랜치는 삭제합니다.

## 커밋 메시지

`타입: 내용` 형식입니다. 타입은 브랜치 접두어와 같은 네 가지를 씁니다.

```
idea: 강의자료 요약 도우미 추가
research: Whisper API 요금·지연 조사
docs: 아이디어 선별 기준 보완
chore: .gitattributes 추가
```

- 한국어로 쓰고, 제목은 50자 안쪽, 끝에 마침표를 붙이지 않습니다.
- 왜 그렇게 했는지가 필요하면 본문에 적습니다. 무엇을 바꿨는지는 diff 를 보면 되니 굳이 나열하지 않아도 됩니다.
- 커밋 하나에 문서 하나가 기본입니다. 아이디어 세 개를 한 커밋에 몰아넣지 않습니다.

## PR

- 제목은 커밋 메시지와 같은 형식으로 씁니다.
- 본문은 PR 템플릿을 채우면 됩니다. 아이디어 추가면 한 줄이어도 충분합니다.
- 머지는 **squash merge** 로 합니다. 히스토리에 문서 하나당 커밋 하나만 남게요.

승인 기준은 무엇을 바꾸는지에 따라 다릅니다.

| 무엇을 바꾸나 | 승인 |
| --- | --- |
| `ideas/`, `research/` 에 새 문서 추가 | 없어도 머지 가능 (셀프 머지 OK) |
| 남이 쓴 문서의 내용 변경 | 원작성자 확인 1명 |
| `README.md`, `CONTRIBUTING.md`, `CONVENTIONS.md` | 팀원 1명 이상 |

새 아이디어를 막지 않는 게 핵심입니다. 쌓는 건 자유롭게, 팀 공통 규칙을 건드릴 때만 합의를 받습니다.

## 리뷰

- **아이디어를 향해 씁니다.** 사람을 평가하지 않습니다.
- **안 되는 이유만 적지 않습니다.** 반대할 땐 "이렇게 바꾸면 될 것 같다" 를 하나 붙여주세요.
- **24시간 안에 리뷰가 없으면** 승인이 필요한 PR도 셀프 머지할 수 있습니다. 본선 전까지 시간이 촉박하니 대기로 멈추지 않습니다.
- 물음표만 남기는 코멘트도 환영합니다. "이거 하루에 되나요?" 가 가장 도움이 되는 질문입니다.

## 라벨

Issue 와 PR 에 상태 라벨을 붙입니다. 값은 문서 앞머리의 `status` 와 같은 어휘를 씁니다.

| 라벨 | 뜻 |
| --- | --- |
| `raw` | 던져만 놓은 것, 검토 전 |
| `exploring` | 살펴보는 중 |
| `shortlist` | 본선에서 꺼내 쓸 후보 |
| `parked` | 지금은 아니지만 버리기 아까움 |
| `dropped` | 접음 |

> 이 라벨들은 레포에 아직 만들어져 있지 않습니다. GitHub 의 Issues → Labels 에서 위 다섯 개를 한 번 만들어주세요.

## Kiro 로 문서를 쓸 때

이 레포에는 [`.kiro/steering/docs.md`](.kiro/steering/docs.md) 가 있어서, Kiro 가 문서를 만들거나 고칠 때 위 규칙과 [CONVENTIONS.md](CONVENTIONS.md) 를 따릅니다. 규칙을 바꾸면 그 파일도 같이 손봐주세요.

생성된 문서는 그대로 올리지 말고 한 번 읽고 올립니다. 특히 **하루 안에 만들 수 있는지** 판단은 사람이 해야 합니다.
