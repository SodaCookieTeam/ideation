# 문서 규칙

문서를 어디에 넣고 어떤 형태로 쓰는지에 대한 규칙입니다. 올리는 절차는 [CONTRIBUTING.md](CONTRIBUTING.md) 에 있습니다.

규칙의 목적은 하나입니다. **본선 당일에 필요한 문서를 몇 초 안에 찾는 것.** 여기에 도움이 안 되는 규칙은 지켜야 할 이유가 없습니다.

## 어디에 넣나

| 경로 | 무엇을 넣나 | 파일명 |
| --- | --- | --- |
| `ideas/` | 아이디어 한 건 = 문서 한 개 | `YYYY-MM-DD-제목.md` |
| `research/` | 조사, 레퍼런스, 기술 검증 기록 | `주제.md` |
| `assets/` | 이미지, 다이어그램, 첨부 | `문서파일명-01.png` |
| 루트 | 레포 운영 문서만 (README, CONTRIBUTING, CONVENTIONS) | 대문자 |

`ideas/` 에만 날짜를 붙입니다. 언제 떠올린 아이디어인지가 정보이기 때문입니다. 조사 자료는 내용으로 찾으니 날짜가 필요 없습니다.

새 폴더를 만들고 싶으면 먼저 물어보세요. 폴더가 늘어나면 찾는 속도가 느려집니다.

## 파일 이름

- 소문자를 쓰고, 단어 사이는 하이픈(`-`)으로 잇습니다.
- **공백과 밑줄은 쓰지 않습니다.** URL 에서 깨지고 터미널에서 다루기 번거롭습니다.
- 한글 파일명도 괜찮습니다. 다만 팀원 중 누구든 URL 로 공유할 일이 있으면 영문을 권합니다.
- 날짜는 `2026-09-09` 형식으로 씁니다. `260909`, `9월9일` 같은 표기는 정렬이 깨집니다.

```
좋음   ideas/2026-09-09-lecture-summarizer.md
좋음   ideas/2026-09-09-강의자료-요약.md
나쁨   ideas/강의자료 요약.md          공백
나쁨   ideas/idea_1.md                 무슨 내용인지 알 수 없음
나쁨   ideas/9-9-요약.md               정렬 깨짐
```

## 앞머리 (front matter)

`ideas/` 문서는 맨 위에 앞머리를 넣습니다. 나중에 상태별로 훑어볼 때 씁니다.

```yaml
---
title: 강의자료 요약 도우미
status: raw
owner: siu717
created: 2026-09-09
tags: [교육, 요약]
---
```

| 키 | 필수 | 값 |
| --- | --- | --- |
| `title` | 필수 | 문서 h1 과 같게 |
| `status` | 필수 | `raw` `exploring` `shortlist` `parked` `dropped` 중 하나 |
| `owner` | 필수 | GitHub 아이디. 질문 받을 사람 |
| `created` | 필수 | `YYYY-MM-DD` |
| `tags` | 선택 | 자유. 나중에 비슷한 것끼리 모아보려고 |

`research/` 문서는 앞머리가 없어도 됩니다.

## 문서 구조

- **h1 은 문서당 하나.** 파일 첫 줄에 오고, 파일명과 내용이 맞아야 합니다.
- **문서 하나에 주제 하나.** 아이디어 두 개가 한 문서에 있으면 나눕니다.
- 제목 계층을 건너뛰지 않습니다. `##` 다음에 `####` 가 오면 안 됩니다.
- 아이디어 문서는 [`ideas/TEMPLATE.md`](ideas/TEMPLATE.md) 의 섹션 순서를 유지합니다. 순서가 곧 판단 순서(문제 → 해결 → 기능 → 실현 가능성)입니다.
- 안 채운 섹션은 지우지 말고 비워둡니다. 빈 섹션이 "여기가 아직 안 풀렸다" 는 표시입니다.

## 쓰는 방식

- 한국어로 씁니다. 기술 용어는 원어를 그대로 써도 됩니다.
- **결론을 먼저 씁니다.** 배경 설명부터 시작하지 않습니다.
- **확인한 것과 추측을 구분합니다.** 안 해본 건 "확인 못 함" 이라고 적습니다. 본선 당일에 이 구분이 시간을 지켜줍니다.
- 한 문장은 한 줄에 씁니다. 보기 좋게 하려고 중간에서 줄바꿈하지 않습니다. diff 가 지저분해집니다.
- 이모지는 쓰지 않습니다.

## 마크다운

- 링크는 상대경로로 씁니다. `[템플릿](ideas/TEMPLATE.md)`
- 코드블록에는 언어를 표시합니다. ` ```yaml `, ` ```bash `
- **이미지에는 대체 텍스트를 반드시 넣습니다.** `![로그인 화면 흐름](assets/login-01.png)` — 화면 낭독기 사용자와 이미지가 안 뜨는 상황을 위한 것이고, 접근성 기본입니다.
- 표는 헤더와 구분선을 갖춥니다.
- 강조는 `**굵게**` 만 씁니다. 밑줄이나 취소선을 장식으로 쓰지 않습니다.

## 고칠 때

남이 쓴 문서를 건드릴 때의 기준입니다.

| 무엇을 | 어떻게 |
| --- | --- |
| 오타, 깨진 링크, 형식 | 바로 수정 PR |
| 사실 오류 (잘못된 API 정보 등) | 바로 수정 PR, 본문에 근거 링크 |
| 판단이나 방향 (기능 범위, 접자는 의견) | 코멘트나 Issue 로 먼저 이야기 |

내 문서든 남의 문서든, **접은 아이디어를 지우지 않습니다.** `status` 를 `dropped` 로 바꾸고 왜 접었는지 한 줄 남깁니다. 파일을 삭제하면 같은 아이디어를 두 달 뒤에 다시 내게 됩니다.

변경 이력은 git 히스토리로 충분합니다. 문서 안에 "수정 기록" 표를 만들지 않습니다.
